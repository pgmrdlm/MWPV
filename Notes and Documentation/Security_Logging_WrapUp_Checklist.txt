Security + Logging Wrap Up — Master Checklist
(Last updated: 2025-08-21 21:12:14Z)
=============================================

1) Logging Tasks
----------------
[x] Verify Logs_Insert_V2.sql loads from SqlCatalog (no hardcoded SQL)
[X] Batch ingest early logs in a single transaction (~200 entries)
[ ] Dedupe by LogGuid or content hash
[x] Final secure-delete sweep of %LOCALAPPDATA%/MWPV/early after PendingCount==0
[ ] Quarantine logic: DPAPI unprotect retry ×3; quarantine if still failing

2) Core Security Polishing
--------------------------
[X] Switch LogRepository payload encryption to LogPayloadKey (drop dual-decrypt)
[ ] Confirm PayloadVer + KeySetVersion bump
[ ] Add password strength meter to SetupPasswordAndKeyFile
[ ] Finalize secure memory wipe routines for shutdown (normal + abnormal)
[ ] Confirm all sensitive temp files are DPAPI-protected or securely wiped

3) Coverage (Audit Reference)
-----------------------------
- Startup: App launch, service wiring, config load
- SetupPasswordAndKeyFile: success, cancel, wrong/missing key file
- DB Open/Close: normal and failed attempts
- Category CRUD: add, update, delete, duplicates
- Export/Backup/Restore: start, success, failure
- Abend & Exceptions: all unhandled errors
- Shutdown: normal and abnormal

4) Hooks & Standards
--------------------
[x] Use central async logger for all post-DB events
- Early-log writer for key-file failures (encrypted with LogPayloadKey)
- Log before showing UI error dialogs
- Include EventId, Severity, Source, UserAction, CorrelationId
- Mask/truncate sensitive values

5) Pre‑Login Logging
---------------------
- Encrypted payload with LogPayloadKey: timestamp, reason, masked hints
- Stored in %LOCALAPPDATA%/MWPV/early-logs/*.elog
- On next good login: scan → ingest to DB → show non-blocking notice → securely delete

6) Schema & Helpers
-------------------
- Logs table: Id, WhenUtc, CreatedUtc, Level, Source, EventCode, SessionId, MachineId, AppVersion, IsCrash, Payload, PayloadFmt, StackHash
- Version indicates which key/encryption method was used
- Helpers: LogEntry DTO, LogMasking.MaskSensitive(), Correlation.BeginScope()
- All log-related SQL (schema, queries) stored in individual .sql files inside encrypted 7z archive, loaded into SecureEncryptedDataStore at runtime

7) Tests
--------
[ ] Wrong key-file password → .elog created (LogPayloadKey) & ingested
[ ] Forced Abend → DB log with stack
[ ] Export to read-only → failure log
[ ] Duplicate category add → info/warn log
[ ] Abnormal shutdown → reason logged
[ ] Decrypt both new LogPayloadKey logs and any older machine-bound logs until retired

8) Secure Deletion & Retention
------------------------------
[x] Overwrite + delete early logs
[x] DB log retention (90 days) + startup purge hooked up (observed: "[LOG Purge] ... retentionDays=90")
[ ] Limit DetailsJson size

9) UI/UX
--------
[ ] Non-blocking notice for prior errors
[ ] Log viewer filters: date, severity, event id, search

10) Quality
-----------
[ ] Clear, actionable entries
[ ] Avoid duplicates
[ ] Await async logs where order matters

11) Housekeeping
----------------
[ ] README section on logging must mention:
    - LogPayloadKey
    - SQL script storage in encrypted archive
    - No direct hardcoding of queries in source
[x] Dev mode: echo logs to debug output
[ ] Unit tests for masking, early-log ingestion, and versioned decryption

Open bug today
--------------
• DEBUG-only helper App.DebugDumpRecentLogs() fails with: Must add values for the following parameters: @FromUtc
  Cause: Updated Logs_Select_Recent.sql now expects @FromUtc; helper didn’t bind it when NULL.
  Fix options (choose one and keep it consistent):
    A) Always add @FromUtc with DBNull.Value (preferred for DEBUG helper).
    B) Change the SQL to "(@FromUtc IS NULL OR CreatedUtc >= @FromUtc)" AND ensure the code always adds the parameter (even if NULL).

Next-session quick start
------------------------
1) Apply tiny fix in App.DebugDumpRecentLogs(): add parameter @FromUtc = DBNull.Value (or a recent cutoff) **unconditionally**.
2) Re-run smoke test; verify the debug log-dump shows rows and no parameter error.
3) If time permits, wire optional UI log viewer filters.

Milestones
----------
Milestone 2:
[ ] Ingest early log test: bad password → .elog created; good login → ingested; early folder empty
[ ] Quarantine test: simulate DPAPI failure → file quarantined; WARN logged

Milestone 3:
[ ] Run through 200+ fake .elog files → single transaction, perf OK
[ ] Verify App.Logout row logged on normal exit (Debug + Release)
