buttons_ToDo.txt

1) Define 3 brush sets
   - Action (red): Normal / Hover / Pressed
   - Category (blue): Normal / Hover / Pressed
   - CategoryItem (green or TBD): Normal / Hover / Pressed

2) Wire styles & triggers
   - Create styles per role (ActionButton, CategoryButton, CategoryItemButton)
   - Each style uses the role’s brushes with Hover/Pressed triggers

3) Apply to action + password buttons
   - Use Action button style for: Add Category, Add Category Item, Create Key File, Submit
   - Use Secondary style (neutral) for Cancel

4) Clean up password UI
   - Align labels/inputs; consistent margins/padding
   - Ensure tab order; focus state (focus ring)
   - Confirm tooltip/validation message layout

5) Migrate Category buttons to dictionary
   - Replace inlined bindings with style keys using Category brush set
   - Keep existing Command/Binding logic intact

6) Sanity pass
   - Hover/Pressed visual feedback on all buttons
   - High-contrast text; check disabled state
   - Verify styles load via App.xaml (no local overrides)
