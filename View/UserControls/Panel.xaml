<UserControl x:Class="MWPV.View.UserControls.Panel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MWPV.View.UserControls"
             xmlns:userControls="clr-namespace:MWPV.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
      
    <Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*" SharedSizeGroup="Catagories">
                </ColumnDefinition>
                <ColumnDefinition  Width="2">
                </ColumnDefinition>
                <ColumnDefinition  Width="50*">
                </ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="left"
                        Name="CategoryPanel">
                <Label Content="List of Password Catagories" 
                    Grid.Row="0" Grid.Column="0" 
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontWeight="Medium"
                    Foreground="Black">
                    <Label.Resources>
                        <Style TargetType="Label">
                            <Setter Property="FontSize" Value="13"/>
                        </Style>
                    </Label.Resources>
                </Label>
                <Button Content="Add Category"
                    x:Name="btnAddCategory"
                    Click="btnAddCategory_Click"
                    Grid.Row="0" Grid.Column="0"
                    Height="20" 
                    Width="130" 
                    Margin="25,0,0,0"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center">
                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color= "#810000" Offset="0"/>
                            <GradientStop Color="#CCCCFF" Offset="0.5"/>
                            <GradientStop Color="#810000" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
            </StackPanel>

            <Button Content="Add Category Item" 
                    Grid.Row="0" Grid.Column="2"
                    Height="20" 
                    Width="130" 
                    x:Name="btnAddCategoryItem"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="center">
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color= "#810000" Offset="0"/>
                        <GradientStop Color="#CCCCFF" Offset="0.5"/>
                        <GradientStop Color="#810000" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
            <!-- Call in user control CategoryGrid-->
            <userControls:CategoryGrid x:Name="CategoryGrid" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
