<UserControl x:Class="MWPV.View.UserControls.Panel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uc="clr-namespace:MWPV.View.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="220"/>
            <ColumnDefinition Width="*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left -->
        <DockPanel Grid.Column="0" Margin="8" LastChildFill="True">
            <TextBlock DockPanel.Dock="Top"
                       Text="Categories"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>
            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        Margin="0,0,0,8">
                <Button x:Name="btnAddCategory"
                        Content="Add Category"
                        Click="btnAddCategory_Click"
                        Style="{StaticResource DialogPrimary}"
                        MinWidth="120"/>
            </StackPanel>

            <!-- Keep this as CatagoryGrid (intentional spelling) -->
            <uc:CatagoryGrid x:Name="CategoryGrid"/>
        </DockPanel>

        <!-- Right -->
        <DockPanel Grid.Column="1" Margin="8" LastChildFill="True">
            <TextBlock DockPanel.Dock="Top"
                       Text="Items"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>
            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        Margin="0,0,0,8">
                <Button x:Name="btnAddCategoryItem"
                        Content="Add Item"
                        Style="{StaticResource DialogPrimary}"
                        MinWidth="120"
                        Visibility="Collapsed"/>
            </StackPanel>
            <ContentControl x:Name="CategoryItemsHost"/>
        </DockPanel>
    </Grid>
</UserControl>
