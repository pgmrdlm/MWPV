TODO LIST - 20250811
--------------------------
1. Flip on logging changes
   - Enable LogPayloadKey usage in logging (post-login DB logs).
   - Tag new log entries with PayloadVer/KeySetVersion.
   - Maintain read access for any older machine-bound entries (temporary).
   - Verify logging paths function after key switch.
   - EARLY-LOGS (pre-DB, pre-keyfile):
     * Implement early-log writer for invalid keyfile password, missing/corrupt keyfile, unreadable DB.
     * Use DPAPI-protected .elog files containing raw INSERT SQL statements (exact format the DB logger uses). These are machine-bound encrypted and never stored in plaintext.
     * Location: %LOCALAPPDATA%/MWPV/early-logs/*.elog
   - INGESTION ON NEXT SUCCESSFUL LOGIN:
     * On successful unlock, scan early-logs directory.
     * Decrypt DPAPI .elog files, re-encrypt into DB using LogPayloadKey, add version tags.
     * Show non-blocking notice: "Previous login errors were ingested."
     * Securely delete .elog files after successful ingestion.
   - EARLY-LOGS PIPELINE (deferred DB inserts via same helper):
     * Store DPAPI-encrypted files containing **parameterized INSERT templates** (no plaintext payloads).
     * On first successful login: decrypt, build JSON payload, **encrypt with LogPayloadKey**, bind parameters, execute.
     * Batch each .elog in a single transaction; limit ~200 entries per file.
     * On failure: move file to early-logs\quarantine and write a WARN DB log.
     * On success: **secure-delete** the .elog.
     * Optional: add LogGuid (TEXT UNIQUE) for dedupe, or compute a hash from (CreatedUtc, EventCode, StackHash, SessionId).

2. Create code for 3 password files
   - DbPassword
   - LogPayloadKey
   - UserSecretsKey
   - Ensure create-if-missing logic.
   - Load into SecureEncryptedDataStore.
   - Wipe all temp buffers after load/create.

3. Quick verification tests
   - Restart path works without re-creating keys.
   - Fresh setup path creates keys once and stores correctly.
   - Simulate invalid keyfile password → .elog created → next login ingests and deletes.
