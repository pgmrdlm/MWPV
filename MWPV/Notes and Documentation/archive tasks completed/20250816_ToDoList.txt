TODO LIST – 2025-08-16
======================

GUIDERAILS FOR TASKS
--------------------
[ ] Smoke test first (60–90 sec)
    - Password window opens; custom title bar works.
    - Enter password → Main window loads; caption icons click.
    - Single-instance guard holds (second launch blocked).
    - Panel renders; no red designer/DB errors.

[ ] Micro-fix → commit/tag → push (no rabbit holes)
    - git add -A
    - git commit -m "Fix: <short description>"
    - git tag -a "MWPV-YYYYMMDD-<ShortDesc>" -m "checkpoint"
    - git push && git push --tags

[ ] Timebox bug hunts
    - Cap at 20 minutes or 3 consecutive errors (whichever first).
    - If tripped: stop, jot a 1-line TODO, revert to last tag, continue.

[ ] Rollback (fast path)
    - git stash -u
    - git checkout tags/MWPV-YYYYMMDD-<Tag> -b hotfix/revert-<Tag>
    - (or) git reset --hard <Tag>

[ ] One goal at a time → finish → checkpoint → next.

[ ] Naming convention
    - Folder: MWPV - YYYYMMDD - <desc>
    - Tag:    MWPV-YYYYMMDD-<DescNoSpaces>

[ ] End-of-session wrap (5 min)
    - Re-run smoke test; git status clean; push + tag.
    - Add 2–3 bullets to tomorrow’s TODO.

PRELUDE (after smoke test)
--------------------------
[ ] Zip source (no bin/obj) and upload for 7z.dll review.
[ ] Gadget scans for hardcoded 7z.dll refs; return arch-aware bootstrap.

LOGGING (umbrella entry)
------------------------
A) Wire logging SQL scripts (no hardcoded SQL)
   [ ] (Optional) Cache insert SQL at construction; warn in DEBUG if any script is missing.

B) Early-log ingestion pipeline
   [ ] START HERE → Batch ingest: single transaction (~200 entries max).
   [ ] Dedupe: prefer LogGuid; else content hash.
   [ ] Final secure-delete sweep of %LOCALAPPDATA%\MWPV\early after PendingCount()==0.

C) Robust delete & edge cases
   [ ] Add final folder sweep as a backstop.
   [ ] DPAPI Unprotect retry logic (up to 3 tries); quarantine if still failing.
   [ ] (Optional) Delete-on-reboot fallback (MoveFileEx).

D) Post-login DB logging changes
   [ ] Switch LogRepository payload encryption to LogPayloadKey (no dual-decrypt).
   [ ] Bump/confirm PayloadVer + KeySetVersion to reflect the new key.
   [ ] Update CryptoLogCodec to use LogPayloadKey for new rows.
   [ ] Manually purge legacy test rows created with machine-bound key (no migration).

E) UX accuracy
   [ ] Popup/log text reflects ingestion result: success / partial / failure, with counts (DEBUG-only is fine).

Acceptance Checks
-----------------
[ ] Good login → batch ingest runs, dedupe applied, early folder ends empty.
[ ] DPAPI throw in Debug → no pause; file quarantined and WARN logged.
[ ] ~200 .elog files → single transaction observed; performance acceptable.
[ ] App.Logout row appears on normal exit (Debug & Release).

NOTE
----
SevenZip DLL path cleanup & test completed on 2025-08-14 — no further work needed unless refactoring ServiceSetUp.
