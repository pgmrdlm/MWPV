<UserControl x:Class="MWPV.View.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="22" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Local-only brushes -->
        <SolidColorBrush x:Key="MenuBg"      Color="#810000"/>
        <SolidColorBrush x:Key="MenuBorder"  Color="#640000"/>
        <SolidColorBrush x:Key="ItemHover"   Color="#8E1A1A"/>
        <SolidColorBrush x:Key="ItemPressed" Color="#5C0000"/>
        <SolidColorBrush x:Key="PopupBg"     Color="#6F0000"/>
        <SolidColorBrush x:Key="PopupBorder" Color="#3A0000"/>

        <Thickness x:Key="TopPadding">12,0,12,0</Thickness>
        <Thickness x:Key="SubPadding">10,6</Thickness>

        <!-- MENU (container) -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="{StaticResource MenuBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Menu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource MenuBorder}"
                                BorderThickness="0,0,0,1">
                            <ItemsPresenter Margin="8,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MENU ITEM (top-level & submenu) -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="{StaticResource TopPadding}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid SnapsToDevicePixels="True">
                            <!-- Top-level header (File/Tools/Help) -->
                            <Border x:Name="TopItemBg"
                                    Background="Transparent"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}"
                                    Margin="2,0"
                                    Visibility="Collapsed">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                            </Border>

                            <!-- Submenu item visuals -->
                            <Border x:Name="SubItemBg"
                                    Background="Transparent"
                                    CornerRadius="6"
                                    Padding="{StaticResource SubPadding}"
                                    Margin="2"
                                    Visibility="Collapsed">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Grid.Column="0"
                                                      Margin="0,0,8,0"
                                                      VerticalAlignment="Center"
                                                      ContentSource="Icon"/>
                                    <ContentPresenter Grid.Column="1"
                                                      VerticalAlignment="Center"
                                                      RecognizesAccessKey="True"
                                                      ContentSource="Header"/>
                                    <ContentPresenter Grid.Column="2"
                                                      Margin="8,0,0,0"
                                                      VerticalAlignment="Center"
                                                      ContentSource="InputGestureText"/>
                                </Grid>
                            </Border>

                            <!-- Submenu popup -->
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}">
                                <Border Background="{StaticResource PopupBg}"
                                        BorderBrush="{StaticResource PopupBorder}"
                                        BorderThickness="0"
                                        CornerRadius="8"
                                        Padding="4">
                                    <StackPanel IsItemsHost="True" Margin="2"/>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Role-driven visibility + states -->
                        <ControlTemplate.Triggers>
                            <!-- Top-level headers -->
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="TopItemBg" Property="Visibility" Value="Visible"/>
                                <Setter Property="Padding" Value="{StaticResource TopPadding}"/>
                            </Trigger>
                            <Trigger Property="Role" Value="TopLevelItem">
                                <Setter TargetName="TopItemBg" Property="Visibility" Value="Visible"/>
                                <Setter Property="Padding" Value="{StaticResource TopPadding}"/>
                            </Trigger>

                            <!-- Submenu entries -->
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="SubItemBg" Property="Visibility" Value="Visible"/>
                                <Setter Property="Padding" Value="{StaticResource SubPadding}"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuItem">
                                <Setter TargetName="SubItemBg" Property="Visibility" Value="Visible"/>
                                <Setter Property="Padding" Value="{StaticResource SubPadding}"/>
                            </Trigger>

                            <!-- Hover / keyboard highlight -->
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="TopItemBg" Property="Background" Value="{StaticResource ItemHover}"/>
                                <Setter TargetName="SubItemBg" Property="Background" Value="{StaticResource ItemHover}"/>
                                <!-- shared accent -->
                                <Setter Property="Foreground" Value="{DynamicResource Interactive.HoverFg}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>

                            <!-- Open submenu (top-level active) -->
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="TopItemBg" Property="Background" Value="{StaticResource ItemHover}"/>
                                <Setter Property="Foreground" Value="{DynamicResource Interactive.HoverFg}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="TopItemBg" Property="Background" Value="{StaticResource ItemPressed}"/>
                                <Setter Property="Foreground" Value="{DynamicResource Interactive.HoverFg}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#88FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- The actual menu -->
    <Grid>
        <Menu>
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Name="mnuFileExit"/>
            </MenuItem>

            <MenuItem Header="_Tools">
                <MenuItem Header="_Backup Password DB"  Name="mnuToolsBackup"/>
                <MenuItem Header="_Restore Password DB" Name="mnuToolsRestore"/>
                <MenuItem Header="_Reset DB Encryption" Name="mnuToolsReset"/>
                <Separator/>
                <MenuItem Header="_View Logs..." Name="mnuToolsViewLogs"
                          Click="mnuToolsViewLogs_Click"/>
            </MenuItem>

            <MenuItem Header="_Help"/>
        </Menu>
    </Grid>
</UserControl>
