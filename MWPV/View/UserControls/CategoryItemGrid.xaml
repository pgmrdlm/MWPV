<!-- File: View/UserControls/CategoryItemGrid.xaml
     FULL REWRITE

     Purpose:
     - Shows Category Items as 3 “pill” buttons per row (Col1/Col2/Col3).
     - Each pill collapses if Content is null/empty.
     - Click bubbles to ItemPill_Click in code-behind.

     Notes:
     - This expects a parent style resource named "ItemPill" to exist (app/theme).
     - ItemsSource binds to BoundCategoryItems on this UserControl (DP/property in code-behind).
-->

<UserControl x:Class="MWPV.View.UserControls.CategoryItemGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">

    <UserControl.Resources>

        <!-- Collapses the pill if Content is null/empty -->
        <Style x:Key="ItemPillWhenNotEmpty"
               TargetType="Button"
               BasedOn="{StaticResource ItemPill}">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Content}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <Trigger Property="Content" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- (Optional) fallback if someone wants to swap styles later.
             Not referenced by default. -->
        <Style x:Key="ItemPillWhenNotEmpty_Fallback"
               TargetType="Button"
               BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Content}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <Trigger Property="Content" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding BoundCategoryItems, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="6,8,6,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Pill 1 -->
                        <Button Grid.Column="0"
                                Style="{StaticResource ItemPillWhenNotEmpty}"
                                Content="{Binding strCategoryItem1}"
                                Tag="{Binding strCategoryItemKey1}"
                                ToolTipService.ToolTip="{Binding strCategoryItemToolTip1}"
                                Click="ItemPill_Click"/>

                        <!-- Pill 2 -->
                        <Button Grid.Column="2"
                                Style="{StaticResource ItemPillWhenNotEmpty}"
                                Content="{Binding strCategoryItem2}"
                                Tag="{Binding strCategoryItemKey2}"
                                ToolTipService.ToolTip="{Binding strCategoryItemToolTip2}"
                                Click="ItemPill_Click"/>

                        <!-- Pill 3 -->
                        <Button Grid.Column="4"
                                Style="{StaticResource ItemPillWhenNotEmpty}"
                                Content="{Binding strCategoryItem3}"
                                Tag="{Binding strCategoryItemKey3}"
                                ToolTipService.ToolTip="{Binding strCategoryItemToolTip3}"
                                Click="ItemPill_Click"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>

</UserControl>
