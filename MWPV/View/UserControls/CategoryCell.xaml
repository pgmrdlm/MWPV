<UserControl x:Class="MWPV.View.UserControls.CategoryCell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MWPV.View.UserControls">

    <UserControl.Resources>
        <local:CategoryCropConverter x:Key="CategoryCrop" />

        <Style x:Key="CategoryCellPill"
               TargetType="Button"
               BasedOn="{StaticResource CategoryPill}">
            <Setter Property="Width" Value="146"/>
            <Setter Property="MinWidth" Value="146"/>
            <Setter Property="MaxWidth" Value="146"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="ToolTipService.ShowDuration" Value="30000"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCategoryActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource DisabledForeground}"/>
                    <Setter Property="BorderBrush" Value="#33000000"/>
                    <Setter Property="Opacity" Value="0.82"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CategoryCellEditButton" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ShadowStroke}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="Background" Value="{DynamicResource PrimaryNormalBrush}"/>
            <Setter Property="ToolTip" Value="Edit category"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsPressed" Value="True"/>
                                    <Condition Property="Tag" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource PrimaryPressedBrush}"/>
                            </MultiTrigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource FocusBorder}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource DisabledBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForeground}"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource DisabledBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForeground}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#33000000"/>
                                <Setter Property="Opacity" Value="0.82"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel x:Name="CellRoot"
                Orientation="Horizontal"
                Width="180"
                Visibility="Visible"
                VerticalAlignment="Center">
        <Button x:Name="CategoryButton"
                Style="{StaticResource CategoryCellPill}"
                Tag="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}"
                Click="CategoryButton_Click">
            <Button.Content>
                <TextBlock Text="{Binding Path=CategoryName, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CategoryCrop}, ConverterParameter='14,11'}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap"
                           HorizontalAlignment="Center"/>
            </Button.Content>
            <Button.ToolTip>
                <ToolTip DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                    <StackPanel>
                        <TextBlock Text="{Binding Path=CategoryName}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Path=CategoryDescription}"/>
                    </StackPanel>
                </ToolTip>
            </Button.ToolTip>
        </Button>

        <Button x:Name="EditButton"
                Style="{StaticResource CategoryCellEditButton}"
                Tag="{Binding IsCategoryActive, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue=True}"
                Click="EditButton_Click"
                AutomationProperties.Name="{Binding Path=CategoryName, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat=Edit {0}}">
            <TextBlock FontFamily="Segoe MDL2 Assets"
                       FontSize="12"
                       Text="&#xE70F;"/>
        </Button>
    </StackPanel>
</UserControl>
