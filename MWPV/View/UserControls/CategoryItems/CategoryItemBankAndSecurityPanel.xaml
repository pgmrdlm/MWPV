<UserControl x:Class="MWPV.View.UserControls.CategoryItems.CategoryItemBankAndSecurityPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="900">
    <UserControl.Resources>

        <!-- Small green icon buttons for Edit/Delete (shared for cards + accounts) -->
        <Style x:Key="BankCardIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Margin" Value="2,0,0,0" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Background" Value="#FF90EE90" />
            <Setter Property="BorderBrush" Value="#FF006400" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Bank cards -->
            <RowDefinition Height="Auto" />
            <!-- Accounts -->
            <RowDefinition Height="Auto" />
            <!-- Security Q/A -->
        </Grid.RowDefinitions>

        <!-- ================= BANK CARDS / ACCOUNTS (CARDS) ================= -->
        <GroupBox Grid.Row="0"
                  Header="Bank Cards / Accounts"
                  Margin="0,0,0,6">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- entry line -->
                    <RowDefinition Height="Auto" />
                    <!-- error -->
                    <RowDefinition Height="Auto" />
                    <!-- grid -->
                </Grid.RowDefinitions>

                <!-- Card entry row -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="170" />
                        <!-- Card type -->
                        <ColumnDefinition Width="170" />
                        <!-- Card number -->
                        <ColumnDefinition Width="140" />
                        <!-- Expiration -->
                        <ColumnDefinition Width="110" />
                        <!-- CVV -->
                        <ColumnDefinition Width="140" />
                        <!-- PIN -->
                        <ColumnDefinition Width="70" />
                        <!-- Active -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Add -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Clear -->
                    </Grid.ColumnDefinitions>

                    <!-- Labels -->
                    <TextBlock Grid.Column="0"
                               Text="Card Type"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />
                    <TextBlock Grid.Column="1"
                               Text="Card Number"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />
                    <TextBlock Grid.Column="2"
                               Text="Expiration Date"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />
                    <TextBlock Grid.Column="3"
                               Text="CVV"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />
                    <TextBlock Grid.Column="4"
                               Text="Card PIN"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />
                    <TextBlock Grid.Column="5"
                               Text="Active"
                               Margin="0,0,0,2"
                               FontWeight="Bold" />

                    <!-- Fields -->
                    <ComboBox x:Name="CardTypeCombo"
                              Grid.Column="0"
                              Margin="0,20,6,0"
                              ItemsSource="{Binding CardTypeItems,
                                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                              DisplayMemberPath="DisplayText"
                              SelectedValuePath="ComboDetailId"
                              SelectedIndex="0"
                              LostFocus="OnBankCardFieldLostFocus" />

                    <TextBox x:Name="CardNumberTextBox"
                             Grid.Column="1"
                             Margin="0,20,6,0"
                             MaxLength="23"                             
                             LostFocus="OnBankCardFieldLostFocus" />

                    <TextBox x:Name="ExpirationTextBox"
                             Grid.Column="2"
                             Margin="0,20,6,0"
                            ToolTip="MM/YYYY"
                            MaxLength="7"
                             LostFocus="OnBankCardFieldLostFocus" />

                    <PasswordBox x:Name="CvvBox"
                                 Grid.Column="3"
                                 Margin="0,20,6,0"
                                  MaxLength="4"
                                 LostFocus="OnBankCardFieldLostFocus" />

                    <PasswordBox x:Name="PinBox"
                                 Grid.Column="4"
                                 Margin="0,20,6,0"
                                 MaxLength="6"
                                 LostFocus="OnBankCardFieldLostFocus" />

                    <CheckBox x:Name="ChkCardActive"
                              Grid.Column="5"
                              Margin="0,24,0,0"
                              IsChecked="True"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Top" />

                    <Button x:Name="BtnBankCardAddOrUpdate"
                            Grid.Column="6"
                            Margin="8,18,0,0"
                            Content="Add"
                            Style="{StaticResource DialogPrimary}"
                            Click="OnBankCardAddOrUpdateClick" />

                    <Button x:Name="BtnBankCardClear"
                            Grid.Column="7"
                            Margin="6,18,0,0"
                            Content="Clear"
                            Style="{StaticResource DialogSecondary}"
                            Click="OnBankCardClearClick" />
                </Grid>

                <!-- Card error message -->
                <TextBlock x:Name="BankCardErrorTextBlock"
                           Grid.Row="1"
                           Margin="0,4,0,4"
                           Foreground="Red"
                           FontWeight="Bold"
                           TextWrapping="Wrap" />

                <!-- Card grid -->
                <DataGrid x:Name="BankCardGrid"
                          Grid.Row="2"
                          Margin="0,10,0,0"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding BankCardRows,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FF800000" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Card Type"
                                            Binding="{Binding CardTypeDisplay}"
                                            Width="*" />
                        <DataGridTextColumn Header="Card Number"
                                            Binding="{Binding CardNumberMasked}"
                                            Width="150" />
                        <DataGridTextColumn Header="Expiration"
                                            Binding="{Binding Expiration}"
                                            Width="120" />
                        <DataGridTextColumn Header="CVV"
                                            Binding="{Binding CvvMasked}"
                                            Width="80" />
                        <DataGridTextColumn Header="PIN"
                                            Binding="{Binding PinMasked}"
                                            Width="80" />
                        <DataGridTemplateColumn Header="Active" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsActive}"
                                              IsHitTestVisible="False"
                                              HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Edit / Delete -->
                        <DataGridTemplateColumn Header="Edit" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource BankCardIconButtonStyle}"
                                            Tag="{Binding}"
                                            Click="OnBankCardEditClick">
                                        <TextBlock Text="✎" FontSize="12" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Del" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource BankCardIconButtonStyle}"
                                            Tag="{Binding}"
                                            Click="OnBankCardDeleteClick">
                                        <TextBlock Text="✖" FontSize="12" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- ======================= ACCOUNTS ======================= -->
        <!-- (unchanged from previous version) -->
        <GroupBox Grid.Row="1"
                  Header="Accounts"
                  Margin="0,4,0,4">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- entry row -->
                    <RowDefinition Height="Auto" />
                    <!-- error -->
                    <RowDefinition Height="Auto" />
                    <!-- grid -->
                </Grid.RowDefinitions>

                <!-- Account entry row -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <!-- Account name -->
                        <ColumnDefinition Width="220" />
                        <!-- Account number -->
                        <ColumnDefinition Width="140" />
                        <!-- Account PIN -->
                        <ColumnDefinition Width="70" />
                        <!-- Active -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Add -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Clear -->
                    </Grid.ColumnDefinitions>

                    <!-- Labels -->
                    <TextBlock Grid.Column="0"
                               Text="Account Name"
                               FontWeight="Bold"
                               Margin="0,0,0,2" />
                    <TextBlock Grid.Column="1"
                               Text="Account Number"
                               FontWeight="Bold"
                               Margin="0,0,0,2" />
                    <TextBlock Grid.Column="2"
                               Text="Account PIN (optional)"
                               FontWeight="Bold"
                               Margin="0,0,0,2" />
                    <TextBlock Grid.Column="3"
                               Text="Active"
                               FontWeight="Bold"
                               Margin="0,0,0,2" />

                    <!-- Fields -->
                    <ComboBox x:Name="AccountNameCombo"
                              Grid.Column="0"
                              Margin="0,20,6,0"
                              ItemsSource="{Binding AccountTypeItems,
                                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                              DisplayMemberPath="DisplayText"
                              SelectedValuePath="ComboDetailId"
                              SelectedIndex="0" />

                    <TextBox x:Name="AccountNumberTextBox"
                             Grid.Column="1"
                             Margin="0,20,6,0" />

                    <PasswordBox x:Name="AccountPinBox"
                                 Grid.Column="2"
                                 Margin="0,20,6,0" />

                    <CheckBox x:Name="ChkAccountActive"
                              Grid.Column="3"
                              Margin="0,24,0,0"
                              IsChecked="True"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Top" />

                    <Button x:Name="BtnAccountAddOrUpdate"
                            Grid.Column="4"
                            Margin="8,18,0,0"
                            Content="Add"
                            Style="{StaticResource DialogPrimary}"
                            Click="OnAccountAddOrUpdateClick" />

                    <Button x:Name="BtnAccountClear"
                            Grid.Column="5"
                            Margin="6,18,0,0"
                            Content="Clear"
                            Style="{StaticResource DialogSecondary}"
                            Click="OnAccountClearClick" />
                </Grid>

                <!-- Account error message -->
                <TextBlock x:Name="AccountErrorTextBlock"
                           Grid.Row="1"
                           Margin="0,4,0,4"
                           Foreground="Red"
                           FontWeight="Bold"
                           TextWrapping="Wrap" />

                <!-- Accounts grid -->
                <DataGrid x:Name="AccountGrid"
                          Grid.Row="2"
                          Margin="0,10,0,0"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding AccountRows,
                                                RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FF800000" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Account Name"
                                            Binding="{Binding AccountTypeDisplay}"
                                            Width="*" />
                        <DataGridTextColumn Header="Account Number"
                                            Binding="{Binding AccountNumberMasked}"
                                            Width="200" />
                        <DataGridTextColumn Header="PIN"
                                            Binding="{Binding PinMasked}"
                                            Width="80" />
                        <DataGridTemplateColumn Header="Active" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsActive}"
                                              IsHitTestVisible="False"
                                              HorizontalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Edit / Delete -->
                        <DataGridTemplateColumn Header="Edit" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource BankCardIconButtonStyle}"
                                            Tag="{Binding}"
                                            Click="OnAccountEditClick">
                                        <TextBlock Text="✎" FontSize="12" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Del" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource BankCardIconButtonStyle}"
                                            Tag="{Binding}"
                                            Click="OnAccountDeleteClick">
                                        <TextBlock Text="✖" FontSize="12" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- ================== SECURITY QUESTIONS / ANSWERS ================== -->
        <Grid Grid.Row="2" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SecurityQuestionAnswerTextBox"
                     Grid.Column="0"
                     Margin="0,0,6,0"
                     Height="26"
                     VerticalContentAlignment="Center"
                     Text="Security question / answer area to be wired later" />

            <Button Grid.Column="1"
                    Content="Add"
                    Style="{StaticResource DialogPrimary}"
                    HorizontalAlignment="Right" />
        </Grid>
    </Grid>
</UserControl>
